apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: courses-api-ingress
spec:
  rules:
    - http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: courses-api
                port:
                  number: 3000
