apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-k8s-innovacion.fullname" . }}
  labels:
    {{- include "course-k8s-innovacion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-k8s-innovacion.selectorLabels" . | nindent 4 }}
